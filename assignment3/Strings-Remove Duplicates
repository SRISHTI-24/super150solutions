#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main() {
	string s;
	cin>>s;
	vector<char> str;
	str.push_back(s[0]);
	for(int i=1;i<s.size();i++)
	{
		if(s[i]!=s[i-1])
		{
			str.push_back(s[i]);
		}
	}
	for(int i=0;i<str.size();i++)
	cout<<str[i];
	return 0;
}
